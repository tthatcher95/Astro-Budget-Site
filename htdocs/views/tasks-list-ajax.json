{"draw":0,"recordsTotal":{{ tasks|length }},"recordsFiltered":{{ tasks|length }},"data":[
{% for task in tasks %}
["<a href='javascript:void(0)' onclick='editTaskDialog({{ task.taskid }})'>{{ task.taskname }}</a>", {{ task.staffing|length }}]{% if loop.last !=true %},{% endif %}
{% endfor %}
]}
